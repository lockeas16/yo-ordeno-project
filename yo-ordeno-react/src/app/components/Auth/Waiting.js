import React from "react";

const Fail = () => (
  <section
    className="uk-section uk-section-large uk-section-muted bg-waiting"
    uk-height-viewport="offset-top: true; expand: true"
  />
);

export default Fail;
